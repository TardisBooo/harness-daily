---
description: 扫描本机 AI coding harness 会话并生成日报
argument-hint: "[--date YYYY-MM-DD]"
---

运行：

```bash
harness-daily report $ARGUMENTS
```

若二进制不在 PATH，先执行 `harness-daily init --host grok`，或使用 `%LOCALAPPDATA%\harness-daily\harness-daily.exe`。
